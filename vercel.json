{
  "version": 2,
  "buildCommand": "pnpm build",
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [{
    "src": "/(.*)",
    "dest": "dist/main.ts",
    "methods": ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS", "HEAD"]
  }]
}
